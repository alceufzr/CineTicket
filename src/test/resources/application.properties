# Desabilita o Flyway durante os testes — evita erro de conexão com banco
spring.flyway.enabled=false

# Desabilita a tentativa de conectar ao PostgreSQL durante os testes
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration